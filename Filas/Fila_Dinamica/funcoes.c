#include <stdio.h>
#include "cabecalho.h"

/*  Funcoes */
void delay(int delay)
{
    for(int i = 0; i < delay; i++)
    {
        for(int j = 0; j < delay; j++)
        {
            /* Delay */
        }
    }
}